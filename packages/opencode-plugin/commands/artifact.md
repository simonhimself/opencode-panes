---
description: Create or revise an OpenCode Panes artifact
---

Fulfill this request by generating complete standalone artifact source, then use the `artifact` tool to create or revise an OpenCode Panes artifact:

$ARGUMENTS

When the request revises an artifact already created in this session, reuse its artifact ID, title, and type. Always send the complete generated source, not the user's prose or a partial patch.
