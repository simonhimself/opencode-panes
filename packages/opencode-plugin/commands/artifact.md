---
description: Create or revise a local-first OpenCode Panes artifact
---

Fulfill this request by using the local-first Panes workflow. Prepare a project-local Artifact, create or import the complete files into its Draft with normal filesystem tools, finalize a Revision, then Sync it only when the user requests a cloud link:

$ARGUMENTS

When the request revises an existing project-local artifact, reuse its artifact ID and prepare a new Draft. Preserve complete file contents and raw bytes. Do not send source strings to legacy cloud creation or revision endpoints.
